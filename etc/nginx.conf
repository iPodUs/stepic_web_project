user              www-data;
error_log         logs/error.log info;

http {
    include       mime.types;
    charset       utf-8;
    access_log    logs/access.log  combined;

    server {
        listen        80 default;

        location      ^~ /uploads/ {
            root      /home/box/web;
        }

		location      ~* ^.+\.jpg$ {
            root      /home/box/web/public;
        }
    }
}